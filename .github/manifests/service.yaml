apiVersion: v1
kind: Service
metadata:
  name: demo-app-merzigo
  namespace: java-demoapp-namespace
  labels:
    app.kubernetes.io/name: demo-app-merzigo
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: demo-app-merzigo
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000